export default class MyPokemon {
    constructor (name) { //Constructor
        this.name = name
    }
    sayHello() { //Funcion saludo =D
        console.log(`Mi pokemon ${this.name} te saluda!!!`)
    }
}